#include <stdio.h>

/**
 *main - outputs lowercase alphabet except e and q.
 *Return: Always 0
 */
int main(void)
{
int n;
for (n = 97; n < 123; n = n + 1)
{
if (n != 101 && n != 113)     
putchar(n);
}
putchar('\n');
return (0);
}